import { Fragment } from 'react';

const Header = (props) => {
  <Fragment>
    <header>
      <h1>React Meals</h1>
      <button>Cart</button>
    </header>
    <div>
      <img />
    </div>
  </Fragment>;
};

export default Header;
